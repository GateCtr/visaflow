Nous avons patché les signatures de Chrome 148 sur 'cev-shared-impit.ts' et optimisé le sniffer. Maintenant, je veux un audit de sécurité anti-bot de bout en bout, sans concession. Tu dois analyser l'intégralité du cycle de vie de la session en partant de l'atterrissage jusqu'au verdict final. 

Inspecte notre code actuel (orchestrateur, gestionnaire de requêtes, configuration Playwright) et compare-le minutieusement aux comportements réels documentés dans nos fichiers JSON (05_integration, 06_captcha, 10_final, etc.).

Analyse la séquence étape par étape et traque la moindre faille, même minime :

1. L'ATTERRISSAGE SUR LA PAGE DE LOGIN / ACCUEIL :
   - Est-ce que notre toute première requête de navigation ('document') vers le site envoie un en-tête 'Referer' ou 'Origin' vide ou parasite ? 
   - Le 'Sec-Fetch-Site' est-il bien égal à "none" sur ce premier jump sans historique, comme le ferait un vrai browser ?
   - Les cookies initiaux (ex: '__RequestVerificationToken', 'TS0110ceb4') sont-ils injectés avec les exacts attributs de casse et de format ?

2. LA NAVIGATION INTERMÉDIAIRE :
   - Lors des passages de pages en pages, est-ce que l'en-tête 'Referer' reflète fidèlement l'URL de la page précédente (ex: avec ou sans le slash final) ?
   - L'ordre des en-têtes pour les requêtes de sous-ressources (images, css, polices) colle-t-il à la réalité du moteur Blink de Chrome 148 ?

3. LE FLUX DU CAPTCHA (LE POST ET LA TRANSITION) :
   - Compare la structure exacte de notre payload de soumission 'POST /Captcha/SetCaptchaToken' avec le JSON réel de juin 1. Le 'Content-Length' est-il calculé nativement ? Est-ce qu'on omet ou ajoute par erreur des en-têtes spécifiques aux formulaires ?
   - Après la validation du captcha, comment le bot gère-t-il la redirection chain vers '/Integration/VOW/' ? Suit-on la redirection via l'API de navigation de Playwright (ce qui peut altérer les en-têtes) ou laisse-t-on le navigateur gérer le 302 de manière purement native ?

4. ANALYSE DU RUNTIME PLAYWRIGHT (EMPREINTE GLOBALE) :
   - Au-delà des en-têtes réseau, y a-t-il des fuites dans l'environnement JS (Navigator, WebGL, permissions, plugins) qui entrent en contradiction avec un Chrome 148 standard sous Windows ?
   - Les délais d'action (typing, clicks, temps d'attente avant soumission) ont-ils une signature trop rectiligne ou prévisible pour le WAF (F5 ASM) ?

Fais un audit ultra-critique. Pour chaque faille potentielle détectée (de la divergence de header mineure au comportement de navigation suspect), liste :
- Le composant / fichier concerné.
- L'écart exact (Ce que fait le bot VS Ce que fait Chrome 148).
- Le correctif précis à appliquer immédiatement.