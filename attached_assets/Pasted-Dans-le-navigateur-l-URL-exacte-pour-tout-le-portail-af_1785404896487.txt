Dans le navigateur l'URL exacte pour tout le portail affiche l'étiquette signsecondeappointement pouvons nous capturer cette url dans le navigateur et voir la route exacte derrière ?
Oui — et on peut même le vérifier sans navigateur, parce que le code Bookitit est public côté client. Regardons d'abord ce qu'on sait déjà :
La réponse est dans notre code — ligne 740. Voici ce qui se passe exactement :
Voilà l'explication complète :

signupsecondappointment n'existe PAS comme endpoint HTTP. C'est uniquement un nom de vue Backbone.js côté client.

Si tu ouvres Chrome DevTools → Network → XHR et que tu remplis le formulaire sur un portail registration_type=2, tu verras exactement :

GET /onlinebookings/signin/?callback=jQuery211...
    &type=default
    &publickey=2d01502f12dc08400e22aea87fb00ae34
    &logintype=document
    &login=TON_MATRICULE
    &password=TON_MOT_DE_PASSE
    &date=2026-09-01
    &time=09:00
    &services[]=bkt853215
    &agendas[]=bkt301070
    &selectedPeople=1
Le widget Backbone fait ce routing interne :

registration_type=2
      ↓
navigate("signupsecondappointment")   ← hash URL dans le browser (#signupsecondappointment)
      ↓
SignUpSecondAppointmentContainer
extends SignInContainer               ← hérite du même submit()
      ↓
fetch → signin/                       ← requête HTTP réelle
C'est pour ça que notre bot appelle directement signin/ — c'est le seul endpoint qui existe derrière cette vue. Le hash #signupsecondappointment est juste un état interne du router client, invisible côté serveur.
A lors c'est le vrai route et non sign first appointment
Exactement — tu as raison. Pour registration_type=2, signin/ est le vrai endpoint que le navigateur utilise. signupfirstappointment/ était un contournement parce que signin/ retournait 0B chez nous.

Le vrai problème est ailleurs : pourquoi signin/ retourne 0B dans notre bot mais répond normalement dans le navigateur.

La cause : le PHPSESSID doit avoir traversé les étapes du widget avant d'accepter signin/ :

Navigateur réel        Notre bot
─────────────────      ──────────────────────
GET /main/             GET /main/ ✅ (scanner)
service sélectionné    (on saute cette étape)
date sélectionnée      (on saute cette étape)
formulaire affiché     (on saute cette étape)
POST signin/ → ✅      POST signin/ → 0B ❌
Le serveur Bookitit tient un état PHP qui suit ces étapes. Si on saute directement à signin/, il ignore la requête.

La vraie solution : avant d'appeler signin/, naviguer via le browser exactement comme le widget — appeler getagendas/, datetime/, puis un appel à selectservice/ ou l'équivalent pour initialiser l'état PHP. Je dois vérifier quel appel exactement "active" le PHPSESSID pour signin/.
Le loader ne fait que 5KB — le vrai bundle est chargé dynamiquement. Regardons dans notre code existant ce qu'on a déjà capturé du flow réseau réel.
Oui, exactement. signin/ est le vrai endpoint — signupfirstappointment/ était un contournement incorrect pour type=2.

Le vrai fix : avant d'appeler signin/, il faut naviguer le browser vers #selecttime/DATE/TIME/AGENDA. C'est ce hash qui déclenche le router Backbone et "chauffe" le PHPSESSID côté serveur — sans ça, signin/ retourne 0B.

La preuve est déjà dans spainPortal.ts ligne 573 — l'ancienne version Playwright faisait exactement ça :

const target = `${base}#selecttime/${date}/${time}${agendaPart}`;
// → router Bookitit active la vue signin/signupsecondappointment
// → PHPSESSID marqué "prêt pour signin/"
// → signin/ répond normalement
La version HTTP actuelle saute cette étape et appelle signin/ directement → 0B.

Le fix concret dans callBookititEndpointBrowser : avant la première tentative signin/, appeler page.evaluate() pour naviguer vers #selecttime/DATE/TIME/AGENDA et attendre que le router charge la vue, puis appeler signin/. Je le fais ?
Fais le
Je lis les deux zones exactes en parallèle avant de toucher quoi que ce soit.
J'ai tout le contexte. Plan exact :

Nouvelle fonction navigateToSelecttime() dans spain-persistent-browser.ts — navigue via location.hash (pas page.goto qui déclenche CF) et attend que le router Backbone resolve vers #signin/#signupsecondappointment
L'appeler dans spain-http-booking.ts juste avant la boucle des candidats auth
Remettre signin/ en premier pour type=2 (le fix correct)
Je lis les deux zones d'insertion en parallèle.
J'ai tout. Je fais les 3 modifications en parallèle.
Je vérifie que useBrowserMode est bien en scope à cet endroit, puis compile.
useBrowserMode n'existe pas — c'est useBrowserCalls. Je corrige et compile.
Les deux erreurs viennent de page.evaluate() qui retourne unknown quand on passe une string. Fix par cast explicite.
TS propre. On lance le test pour valider que signin/ répond maintenant après le warmup #selecttime.
La navigation #selecttime a bien fonctionné — le router a confirmé #signupsecondappointment. Mais signin/ retourne toujours 0B. Voilà pourquoi :

navigateToSelecttime → #signupsecondappointment  ✅  (router client activé)
signin/ fetch → 0B                                ❌  (état serveur pas initialisé)
jQueryAjax signin/ → __ERR_JQUERY_SCRIPT_TIMEOUT  ❌  (serveur n'appelle pas le callback)
La navigation location.hash active le router Backbone côté client uniquement. Le serveur PHP n'en sait rien. Pour que signin/ réponde, le PHPSESSID doit avoir traversé une requête HTTP intermédiaire — probablement un selectservice/ ou selectpeople/ que le widget appelle entre la sélection du créneau et l'affichage du formulaire. Je vérifie.
CF bloque le curl direct. Je regarde ce que le vrai widget appelle en analysant notre intercepteur de requêtes existant.
Le router confirme #signupsecondappointment mais signin/ reste muet. Je dois voir exactement quelles requêtes HTTP le widget fait pendant la transition — en capturant le réseau en temps réel.
Dans le navigateur on affiche une date avec une liste des heures disponibles, un clic sur une heure ramène sur signsecondappointmenr
C'est le clic sur l'heure qui déclenche tout — le widget appelle probablement freetempevent/ pour réserver temporairement le créneau ET initialiser l'état PHP, puis navigue vers #signupsecondappointment. Sans ce call HTTP, le serveur ignore signin/. freetempevent/ est dans notre liste de suffixes connus — c'est la pièce manquante.
freetempevent/ — c'est ça. C'est ce que le widget appelle quand tu cliques sur l'heure. Je lis les params exacts en parallèle.
freetempevent/ est utilisé pour libérer un slot (annulation) — pas pour le créer. La capture de Cuba le confirme. Le vrai call qui initialise le state PHP au clic sur l'heure est probablement dans le widget JS lui-même. Je vais l'intercepter en temps réel dans le browser pour voir exactement ce qui est envoyé.
You've reached your daily free quota limit. It will reset at 12:00 AM UTC.