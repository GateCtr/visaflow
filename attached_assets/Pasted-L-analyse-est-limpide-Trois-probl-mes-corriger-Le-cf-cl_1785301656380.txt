L'analyse est limpide. Trois problèmes à corriger :

    Le cf_clearance "fantôme" — réutilisé depuis le profil sans JSD frais → "cookie fantôme"
    Le nonce figé vient du localStorage/IndexedDB des partitions CF — Storage.clearDataForOrigin ne couvre pas les partitions blob/worker de Chromium → il faut purger les répertoires sur disque
    --disk-cache-size=0 casse les scripts CF — jsd/main.js et b0da9f4911ba/main.js ne se chargent plus → JSD oneshot jamais envoyé → timeout 6s

Plan : retirer --disk-cache-size=0, étendre la purge disque aux storages CF, et annuler /main/ si JSD oneshot échoue.

Trois corrections en parallèle :

Maintenant les deux autres corrections en parallèle — retirer --disk-cache-size=0 et mettre à jour l'intercepteur pour annuler /main/ si JSD échoue :

Maintenant mettre à jour l'intercepteur pour annuler /main/ si JSD oneshot échoue :

Je mets à jour en parallèle le handler JSD oneshot (pour marquer jsdOneShotAccepted) et l'intercepteur Fetch (pour annuler /main/ si JSD a échoué) :

Maintenant mettre à jour l'intercepteur pour annuler /main/ si JSD a échoué :
Running the final test
$ cd artifacts/slot-hunter && CAPSOLVER_API_KEY="$(printenv CAPSOLVER_API_KEY)" \ DECODO_PROXY_URL="$(printenv DECODO_PROXY_URL)" \ CHROMIUM_EXECUTABLE_PATH="/home/runner/.cache/puppeteer/chrome/linux-149.0.7827.22/chrome-linux64/chrome" \ node_modules/.bin/tsx test-spain-pb.ts 2>&1
Investigating persistent session nonce source

Le ♻️ cf_clearance déjà présent dans le profil après purge révèle le problème suivant : le fichier Default/Cookies sur disque n'est pas purgé. Chrome le relit au démarrage → cf_clearance et PHPSESSID stale sont rechargés → CF sert le même script JSD empoisonné pour cette session connue. Le nonce 1785297931 est figé depuis 33 min dans ce cookie fantôme.

Il faut purger Default/Cookies pour forcer CF à émettre un vrai cf_clearance frais via JSD de zéro.
Verifying the cookie purge fix
$ cd artifacts/slot-hunter && CAPSOLVER_API_KEY="$(printenv CAPSOLVER_API_KEY)" \ DECODO_PROXY_URL="$(printenv DECODO_PROXY_URL)" \ CHROMIUM_EXECUTABLE_PATH="/home/runner/.cache/puppeteer/chrome/linux-149.0.7827.22/chrome-linux64/chrome" \ node_modules/.bin/tsx test-spain-pb.ts 2>&1

You've reached your daily free quota limit. It will reset at 12:00 AM UTC.