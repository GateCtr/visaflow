L'analyse des vecteurs de blocage non explorés met en évidence des asymétries critiques de protocoles et d'infrastructure. Nous devons immuniser le bot immédiatement avant le prochain run. Applique les corrections suivantes point par point :

1. FIX #2 : CORRECTION DES HEADERS DU POST LOGIN (URGENT)
   - Emplacement : 'cevHttpSetup.ts' (lignes 240–257)
   - Problème : Le POST du formulaire de connexion utilise par erreur la branche AJAX de 'getCevBrowserHeaders', envoyant des en-têtes 'cors' / 'empty' incohérents avec une soumission de formulaire HTML.
   - Action : Ajoute un drapeau ou modifie les options passées à 'getCevBrowserHeaders' (ex: 'isFormPost: true') pour forcer le mode document standard :
     * Sec-Fetch-Mode: "navigate"
     * Sec-Fetch-Dest: "document"
     * Sec-Fetch-User: "?1"
     * Ordre des en-têtes : 'Sec-Fetch-*' positionnés APRÈS 'sec-ch-ua*'.

2. FIX #3 : PURGE ET VERSIONING DU CACHE REDIS (URGENT)
   - Emplacement : 'cev-redis-persistence.ts' (ou l'orchestrateur de session)
   - Problème : Les sessions persistées au cours des dernières 25 heures ne contiennent pas le cookie F5 'TS0110ceb4' (fixé récemment), ce qui va corrompre les requêtes actives.
   - Action : Incrémente ou ajoute un attribut 'version: 2' dans l'interface/type 'SerializableVowintSession'. Tout enregistrement récupéré depuis Redis possédant une version inférieure ou absente doit être immédiatement invalidé et forcer une reconnexion complète pour reconstruire un jar propre. (Alternative : vide manuellement les clés 'visaflow:cev-vowint:*').

3. FIX #4 : GESTION EXPLICITE DU VERDICT CAPTCHA
   - Emplacement : 'cevHttpSetup.ts' (ligne 676)
   - Problème : Le bot assume que HTTP 200 = Captcha validé. Or, le serveur renvoie un 200 même si 'captchaSolved: false'.
   - Action : Valide explicitement le booléen de la réponse. Si 'captchaData.captchaSolved === false', lève une erreur explicite : "Captcha validation rejected by server".

4. FIX #5 : SÉCURISATION DU FALLBACK DE REDIRECTION
   - Emplacement : 'cevHttpSetup.ts' (ligne 374)
   - Problème : 'resolveFirstAppIdFromMyList' utilise encore 'redirect: "follow"'.
   - Action : Aligne ce code sur le pattern robuste déjà appliqué au login : passe en 'redirect: "manual"' et gère la boucle de redirection de manière cumulative pour ne perdre aucun cookie de session ou de rafraîchissement F5.

5. AUDIT & FIX #1 & #6 : EMPREINTE CHROME & COHÉRENCE TLS
   - Vérifie dans la documentation ou l'implémentation d'impit@0.14.0 si une configuration explicite du profil TLS (JA3/JA4/ALPN) pour Chrome 148 est supportée. Si impossible, assure-toi que l'empreinte TLS générée par défaut ne trahit pas une version archaïque.
   - Dans notre pool de signatures, ajoute la troisième brand absente pour correspondre à un Chrome officiel stable sous Windows : '"Google Chrome";v="148"'.

Exécute ces modifications en priorité et confirme lorsque la structure globale est alignée.