Tu as raison. Le JSD oneshot est bien détecté, construit et exécuté (HTTP 200 ✅). Le problème c'est que le code invalide la session avec le message "JSD non complété" alors que le JSD a réussi. C'est une mauvaise interprétation du 0B — il faut regarder pourquoi /main/ retourne quand même 0B malgré le JSD réussi.


3 actions
Tu as raison — le JSD oneshot est construit et fire avec HTTP 200. L'invalidation est une fausse erreur. Deux vrais problèmes à corriger :

Même nonce les deux fois (0.8643838971788047:1785222031) — jsd/main.js est servi depuis le cache CF, le nonce est déjà consommé au 2ème essai
POST widget #2 → 1559B au lieu de ~2497B — le token CSRF est probablement à usage unique
Je lis les deux sections concernées :


3 actions
Deux fixes à faire :

Cache-bust jsd/main.js — même nonce réutilisé = nonce déjà consommé au 2ème essai
Corriger le message d'erreur — le "JSD non complété" est faux quand le JSD a réussi

3 actions
Maintenant on relance le probe Kinshasa pour voir si le nonce frais débloque /main/ :


2 actions
Le nonce est toujours 0.8643838971788047:1785222031 — 12h d'âge (Jul 27 18:00 UTC). CF sert le même jsd/main.js lié au cf_clearance CapSolver, le cache-bust ?t= est ignoré. Je lis le flow exact de session mémoire et la description JSD pour comprendre le vrai mécanisme :


3 actions
Deux informations critiques dans la mémoire :

JSD oneshot body : "Fire best-effort with CSRF token as body" — on envoie "" au lieu de token=<csrf>
RUM beacons manquants : "Un bot HTTP qui ne fire jamais ces beacons = signal JS non exécuté → shadow-ban progressif" — beacons #20, #24, #29 sont critiques

3 actions
La mémoire dit que fireRumBeacon est implémenté, mais le grep montre seulement 2 lignes rum dans le scanner. Je vérifie les deux points en parallèle — l'état réel des beacons et la ligne JSD body :


3 actions
Les beacons ne sont pas implémentés — le commentaire ligne 838 le confirme. Je lis les zones d'insertion exactes en parallèle :


3 actions
Maintenant j'ai tout ce qu'il faut. Deux causes du 0B :

JSD oneshot body vide — doit envoyer token=<csrf> (mémoire: "best-effort with CSRF token")
RUM beacons absents — beacon #29 après /main/ est critique pour le score CF, #20 et #24 aussi
Je fais tous les changements en une fois :


2 actions
Je fais tous les changements en parallèle :


2 actions
Maintenant j'ajoute les beacons aux bons endroits et corrige les bodies JSD. Je lis les lignes exactes en parallèle :


16 actions
Maintenant beacon #29 (critique, après /main/) et #109 (après companions) :


Show less

Edited spain-http-scanner.ts
Verifying code and running probe

Verifying code and running probe
Vérification TypeScript puis relance du probe :


Ran src/spain-http-scanner.ts
$ cd artifacts/slot-hunter && node_modules/.bin/tsx --noEmit src/spain-http-scanner.ts 2>&1 | head -30 || node_modules/.bin/tsc --noEmit 2>&1 | grep "spain-http-scanner" | head -20
You've reached your daily free quota limit. It will reset at 12:00 AM UTC.