D'accord — je reste sur test-saopola-live.ts. Le vrai problème identifié : executeHttpBooking utilise impit avec un PHPSESSID froid (le contexte incognito navigue vers /main/ mais le widget JS ne tourne pas, donc le serveur ne connaît pas la session). La session principale du browser, elle, est chaude (le widget a tourné et les XHR ont été capturés). Le fix : faire passer les appels JSONP du booking par callBookititEndpointViaBrowser quand on est en mode persistent-browser.

Le fix à faire : dans spain-http-booking.ts, quand session.source === "playwright", utiliser callBookititEndpointViaBrowser (session principale chaude du browser) au lieu d'impit avec le PHPSESSID froid du contexte incognito. Je lis la fin de la fonction pour tout avoir.

Le fix est clair : quand session.source === "playwright", bypasser complètement le contexte incognito froid et router tous les appels JSONP du booking via callBookititEndpointViaBrowser (session principale chaude du widget). Je fais le fix minimal dans spain-http-booking.ts.

Maintenant j'ajoute le helper browser et je modifie executeHttpBooking pour router via le browser quand source === "playwright".

Maintenant je modifie executeHttpBooking pour bypasser le contexte incognito et utiliser le browser pour tous les appels JSONP quand source === "playwright".

Maintenant je remplace tous les callBookititEndpoint(bookingSession, ...) dans le flow par le routing conditionnel browser/impit.

Je continue — remplacer les appels datetime/, auth candidates, confirmclient/ et summary/ en parallèle.

Compile check + test en parallèle.

Zéro erreur nouvelle. On lance le test.

Progression : getagendas/ et getwidgetconfigurations/ reviennent maintenant du cache ✅ (176B, 371B). Mais signin/ retourne encore 0B même via browser. C'est un vrai problème de flow serveur. Je dois voir le corps brut de ces réponses et comprendre pourquoi signin/ refuse. J'ajoute des logs de debug temporaires directement dans executeHttpBooking.

J'ajoute aussi un log du corps brut de signin/ avant le parsing, puis je relance le test.

peut etre qu'il faut simuler le clic sur comme les fais les autres?
18 minutes ago

peut etre qu'il faut simuler le clic sur comme les fais les autres?en plus l'identifiants pour saopolo c'est numero matricule et mot de passe et nom email, pour celui de kinshassa c'est le numero de passeport et le mot de passe
15 minutes ago
Debugging JSONP parsing regex failure

J'ai tout ce qu'il faut. Deux problèmes distincts dans le log :

    parseJsonpResponse retourne null sur le body callback=jQuery...({"Agendas":[...]}) — le body du cache a le préfixe callback= que la regex ne gère pas → agendaId reste vide → agendas[] absent du signin/ → le serveur retourne 0B
    Credentials Saopola : matricule + mot de passe (pas email/passeport)

Le fix principal : parseJsonpResponse doit stripper le préfixe callback= avant de parser.

Maintenant je mets aussi à jour le test avec un matricule fake réaliste pour Saopola (pas un email), et je relance.
