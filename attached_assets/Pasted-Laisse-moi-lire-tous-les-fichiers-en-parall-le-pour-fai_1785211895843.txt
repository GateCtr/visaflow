Laisse-moi lire tous les fichiers en parallèle pour faire une comparaison complète :

Analyse complète faite. Il y a des différences importantes entre ce que nos appels envoient et ce que le vrai navigateur Cuba envoie. Voici le tableau comparatif :
✅ Ce qui est identique
Élément	Cuba Burp	Notre scanner
Endpoint getagendas/	✅	✅
Endpoint datetime/	✅	✅
Headers Sec-Fetch, XHR, Accept	✅	✅
Cookies _ga, PHPSESSID, cf_clearance	✅	✅
selectedPeople=1	✅	✅
❌ Différences critiques
1. Noms des paramètres de date — bug confirmé
	Cuba réel	Notre scanner
Début	start=2026-08-01	date_from=2026-08-01
Fin	end=2026-08-31	date_to=2026-08-31

Le serveur PHP ne reconnaîtra pas date_from/date_to → la réponse contiendra probablement tous les jours ou rien.
2. Notation tableau pour services[] et agendas[]
	Cuba réel	Notre scanner
Services	services%5B%5D=bkt897578	services=bkt897578
Agendas	agendas%5B%5D=bkt316096	agendas=bkt897578

PHP distingue $_GET['services'] (string) de $_GET['services'][] (array). Bookitit attend probablement un tableau.
3. Params manquants dans nos appels

Cuba envoie aussi type=default, version=4, src=https://..., srvsrc=https://www.citaconsular.es dans getagendas/ et datetime/. Notre scanner les omet.
4. Format des serviceId dans le HTML /main/

La réponse getservices/ Cuba retourne id: "bkt897578" (avec préfixe bkt). Notre regex #selectservice\/(\d+) ne capture que des chiffres — si le HTML de /main/ Cuba rend #selectservice/bkt897578, on raterait complètement le service.
Conclusion

Le flow Cuba fonctionnerait partiellement avec notre scanner actuel (même portail Bookitit, même structure générale), mais les bugs sur start/end et services[] feraient que datetime/ renverrait des résultats incorrects ou vides même si des créneaux existent. Faut-il que je corrige confirmSlotsViaDatetime pour aligner sur les vraies requêtes Cuba ?

si c'est le meme portail c'est à dire qu'il n'y a pas de difference en terme de code ?
6 minutes ago
Comparing API parameters

Oui, c'est exactement la même plateforme Bookitit — même backend PHP, même JS client pour tous les consulats. Donc les paramètres API sont universels, pas spécifiques à Cuba.

Ce que ça implique concrètement :

start/end sont les vrais paramètres Bookitit. Nos date_from/date_to dans confirmSlotsViaDatetime sont une erreur de notre côté — probablement une déduction qu'on avait faite sans capture Burp à l'époque. Pour Kinshasa ça n'a pas posé problème jusqu'ici parce que la détection passe par "No hay horas disponibles" dans le HTML de /main/ avant d'appeler confirmSlotsViaDatetime — autrement dit confirmSlotsViaDatetime n'a jamais été vraiment testé en production.

Quand un créneau existe vraiment (Cuba en août 2026 par exemple), on tombera dans confirmSlotsViaDatetime et là les mauvais noms de params feront que datetime/ répondra mal → faux négatif : on rate un créneau réel.

Les autres diffs (services[], agendas[], type, version, src, srvsrc) sont aussi universelles — le vrai client Bookitit envoie toujours ces params.

Je corrige confirmSlotsViaDatetime maintenant pour aligner sur les vraies requêtes Burp :

Je fais les corrections maintenant — alignement complet sur les vraies requêtes Burp Cuba (qui sont universelles Bookitit) :

Maintenant corrige aussi le regex d'extraction des serviceId dans le HTML — il faut accepter le format bkt123 en plus des IDs numériques, puis vérifie la compilation :

You've reached your monthly free quota limit. It will reset on August 26, 2026.