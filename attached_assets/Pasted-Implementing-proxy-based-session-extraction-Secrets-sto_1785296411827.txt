Implementing proxy-based session extraction

Secrets stockés. J'installe node-fetch et j'écris le test Oxylabs en parallèle.

Je regarde comment spainCfFetch appelle impit, puis j'écris le test Oxylabs.

Je cherche spainCfFetch et le schéma impit en parallèle.

J'ai tout ce qu'il me faut. Je regarde juste comment instancier impit directement, puis j'écris le test.
Mapping the proxy integration workflow
Formulating web scraping strategy

J'ai tout le contexte. J'écris le test Oxylabs maintenant.
Executing the test file

The test file is written. Now I need to run it. Let me execute it with the watchdog.
$ cd artifacts/slot-hunter && OXYLABS_USERNAME=$(printenv OXYLABS_USERNAME) OXYLABS_PASSWORD=$(printenv OXYLABS_PASSWORD) node_modules/.bin/tsx test-spain-oxylabs.ts 2>&1
Analyzing Oxylabs 550 error codes

Status 550 = Oxylabs n'a pas pu délivrer la requête. Deux causes possibles : JS rendering pas activé, ou l'URL est bloquée par leur système. Je teste d'abord une URL simple pour distinguer les deux cas, puis j'essaie les headers Oxylabs pour le JS rendering.

la docs dit ceci: If Web Unblocker is being used to scrape websites dependent on loading data via JavaScript, refer to the JavaScript rendering section. The product is not designed to be used with headless browsers (e.g., Chromium, PhantomJS, Splash, etc.) and their drivers (e.g., Playwright, Selenium, Puppeteer, etc.) directly.
18 minutes ago

Clair. Il faut X-Oxylabs-Render: html pour le JS rendering (CF challenge inclus), timeout client 180s. Je mets à jour le test.

Il me reste à ajouter le header X-Oxylabs-Render: html et le timeout 180s sur le fetch du portail.

550 constant même avec X-Oxylabs-Render: html. Je teste d'abord si le produit Web Unlocker est actif sur ce compte avec une URL simple, puis je teste citaconsular sans rendering pour isoler le problème.

Je crée un fichier de diagnostic rapide plutôt que d'utiliser -e.

Diagnostic complet. Les résultats sont nets :
Test	Status	Conclusion
httpbin sans/avec render	200 ✅	Web Unlocker actif sur le compte
citaconsular.es racine sans render	200, 0B	CF strip body
citaconsular.es racine avec render	200, 39B vide	Oxylabs passe CF mais PHP retourne un shell vide — pas de PHPSESSID
citaconsular.es widget avec render	550	Oxylabs bloqué spécifiquement sur cette URL

Bilan des 3 approches testées :
Approche	Résultat
CapSolver + Chromium (actuel)	cf_clearance lié au TLS CapSolver → /main/ = 0B
Chromium natif + Decodo	CF lance un Turnstile interactif → Chromium headless bloqué
Oxylabs Web Unlocker	550 sur le widget, 39B (HTML vide) sur la racine — pas de PHPSESSID

Le problème de fond : citaconsular.es a une double protection — CF (cf_clearance) et une session PHP initialisée par le widget JS (PHPSESSID avec les bonnes clés). Ces deux éléments doivent venir du même browser. Aucune approche HTTP-only ne peut satisfaire les deux à la fois.

La voie la plus réaliste est de laisser CapSolver résoudre CF directement dans notre Chromium — c'est ce que leur type de tâche TurnstileTask permet (ils injectent le token dans notre page plutôt que de solve côté leur infra). Veux-tu qu'on explore ça ?

testons avec celui de decodo, ouvre la fenetre je te passe le bear token,
13 minutes ago
Add secrets
DECODO_UNLOCKER_TOKEN
Secret provided
Verifying Decodo proxy configuration

Token stocké. Je regarde d'abord le format exact de l'API Decodo Web Unlocker pour construire le bon test.

scraper-api.decodo.com POST → 200 ✅. Je récupère la réponse complète pour voir le format exact avant d'écrire le test.

Format Decodo API clair :

    POST scraper-api.decodo.com/v2/scrape
    Réponse JSON : results[0].content (HTML) + results[0].cookies (tableau) + results[0].status_code

J'écris le test directement.

Code 613 = citaconsular.es est explicitement bloqué par Decodo (site sur leur liste noire). Même résultat qu'Oxylabs (550).

Bilan complet des 4 approches testées :
Approche	Résultat	Raison
CapSolver + Chromium (actuel)	/main/ = 0B	cf_clearance lié au TLS CapSolver ≠ notre Chromium
Chromium natif + Decodo ISP	Bloqué étape CF	Turnstile interactif, headless détecté
Oxylabs Web Unlocker	550	citaconsular.es blacklisté
Decodo Scraper API	613	citaconsular.es blacklisté explicitement

Le site bloque tous les scrapers commerciaux connus. La seule voie qui reste est de résoudre CF à l'intérieur de notre propre Chromium — CapSolver a un mode pour ça : AntiCloudflareTask avec type: "turnstile" qui injecte le token Turnstile directement dans la page Puppeteer au lieu de faire le solve côté leur infra. Le cf_clearance serait alors émis pour notre TLS Chromium.

Tu veux qu'on creuse cette piste ?